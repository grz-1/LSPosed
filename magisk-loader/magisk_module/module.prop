id=${moduleId}
name=LSPosed IT (GitHub@LSPosedDevelopers)
version=${versionName} (${versionCode})
versionCode=${versionCode}
author=${authorList}
description=Another enhanced implementation of Xposed Framework. Supports Android 8.1 ~ 16. Requires Magisk 27.0+ and Zygisk enabled.
updateJson=${updateJson}
validated=1145141919810
IT=I Tested (or Internet Tested) ≠ Internal Test, so It ≈ a fake LSPosed Internal Test module.
joinITedGroup=Telegram @LSPesod_bot

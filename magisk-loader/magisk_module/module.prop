id=${moduleId}
name=LSPosed IT (GitHub@LSPosedDevelopers)
version=${versionName} (${versionCode})
versionCode=${versionCode}
author=${authorList}
description=Another enhanced implementation of Xposed Framework. Supports Android 8.1 ~ 16. Requires Zygisk enabled.
updateJson=${updateJson}
validated=0
IT=I Tested (or Internet Tested) ≠ Internal Test, so It is not an LSPosed Internal Test module, please be careful to distinguish.
joinTheVersionGroup=Telegram @LSPesod_bot
